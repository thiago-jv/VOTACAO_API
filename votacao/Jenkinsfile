pipeline {
    agent any
    stages {
        stage ('Build Backend'){
            steps {
                sh '-f /var/lib/jenkins/workspace/DeployBackEnd/votacao/pom.xml mvn clean package -DskipTests=true'
            }
        }
    }
}