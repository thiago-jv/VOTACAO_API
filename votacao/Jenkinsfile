pipeline {
    agent any
    stages {
        stage ('Build Backend'){
            steps {
                sh '/var/lib/jenkins/workspace/Pipeline/votacao/pom.xml mvn clean package -DskipTests=true'
            }
        }
    }
}